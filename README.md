# 🎓 PSQL Mock Creator - Генератор учебных баз данных PostgreSQL

Инструмент для создания учебных баз данных PostgreSQL с графическим и консольным интерфейсом. Идеально подходит для обучения SQL, разработки и тестирования.

## ✨ Возможности

### 🖥️ **Графический интерфейс (PyQt6)**
- 📝 Текстовые поля для настройки подключения к PostgreSQL
- ✅ Чекбоксы для выбора создаваемых баз данных
- 🎯 Кнопки: "Создать базы данных" и "Очистить базы данных"
- 📟 Встроенная консоль вывода с подсветкой сообщений
- 💾 Сохранение конфигурации между сессиями

### ⌨️ **Консольный интерфейс (CLI)**
- 🚀 Быстрое создание/очистка баз из командной строки
- 🔧 Гибкие параметры запуска
- 📊 Автоматическое логирование операций

## 📊 **Доступные базы данных**

### 🎮 `games_easy` - Простая база видеоигр
- **Таблицы**: games (1 таблица)
- **Уровень**: Начальный
- **Идеально для**: базовых SELECT, WHERE, ORDER BY

### 🏫 `school_world` - Школьная база данных  
- **Таблицы**: teachers, classes, students, subjects, grades (5 таблиц)
- **Уровень**: Средний
- **Идеально для**: JOIN, агрегатных функций, группировки

### 🛒 `games_shop` - Магазин видеоигр
- **Таблицы**: games, customers, orders, order_items (4 таблицы)
- **Уровень**: Продвинутый
- **Идеально для**: сложных JOIN, подзапросов, анализа данных

### ✈️  `air_travel` - Авиакомпании и авиаперелеты
- **Таблицы**: airlines, airports, aircraft, flights, passengers (5 таблиц)
- **Уровень**: Продвинутый
- **Идеально для**: сложных JOIN, временных данных, внешних ключей, анализа маршрутов

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка PostgreSQL

Создайте файл `config/postgres.json` или используйте настройки по умолчанию:

```json
{
    "user": "postgres",
    "password": "ваш_пароль",
    "host": "localhost",
    "port": 5432
}
```

### 3. Запуск графического интерфейса

```bash
python main.py
```

### 4. Запуск консольной версии

```bash
# Создать все базы данных
python cli.py --create

# Создать только games_easy и school_world
python cli.py --create games_easy school_world

# Очистить все базы
python cli.py --clean

# Очистить только air_travel
python cli.py --clean air_travel

# Показать список доступных баз
python cli.py --list

# Показать текущую конфигурацию
python cli.py --config
```

## 📁 Структура проекта

```
psql-mock-creator/
├── core/                    # Ядро приложения
│   ├── config_manager.py    # Управление конфигурацией
│   ├── database_manager.py  # Логика работы с БД
│   └── logger.py           # Перехват и логирование вывода
├── ui/                     # Графический интерфейс
│   ├── main_window.py      # Главное окно PyQt6
│   └── styles.py          # CSS стили интерфейса
├── models/                 # Модели Peewee для каждой БД
├── mock_data/             # Тестовые данные в формате JSON
├── config/                # Конфигурационные файлы
│   └── postgres.json      # Настройки подключения к PostgreSQL
├── main.py                # Точка входа для GUI
├── cli.py                 # Точка входа для CLI
├── requirements.txt       # Зависимости Python
├── version.py             # Версия приложения
└── README.md             # Эта документация
```

## 🛠️ Требования

- **Python 3.8+**
- **PostgreSQL 12+**
- **Доступ к серверу PostgreSQL** с правами на создание баз данных

### Установленные зависимости

```txt
peewee # ORM для работы с PostgreSQL
psycopg2 # Адаптер PostgreSQL для Python
PyQt6 # Графический интерфейс
```

## 💡 Примеры использования

### Графический интерфейс
1. Запустите графический интерфейс
2. Настройте подключение к вашему серверу PostgreSQL
3. Выберите базы данных для занятий
4. Нажмите "Создать базы данных"
5. Раздайте студентам настройки подключения

### Командная строка
```bash
# Быстрое создание тестовых баз
python cli.py --create games_shop air_travel

# Очистка после тестирования
python cli.py --clean games_shop air_travel

# Проверка подключения
python cli.py --config
```

## 🔧 Настройка

### Конфигурационный файл

Файл `config/postgres.json` автоматически создается при сохранении настроек из интерфейса:

```json
{
    "user": "postgres",
    "password": "secure_password",
    "host": "localhost",
    "port": 5432
}
```

### Параметры подключения

| Параметр | По умолчанию | Описание                 |
|----------|--------------|--------------------------|
| host     | localhost    | Адрес сервера PostgreSQL |
| port     | 5432         | Порт подключения         |
| user     | postgres     | Имя пользователя         |
| password | (пусто)      | Пароль пользователя      |

